-- AGGREGATE Functions
-- > calculation on set of values, returns a single values
-- > cout(),max(),min(),sum(),avg()

SELECT max(marks)
FROM StudentInfo;

SELECT avg(marks)
FROM StudentInfo;

SELECT count(name)
FROM StudentInfo;

